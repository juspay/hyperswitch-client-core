// SKIP-REASON: File is empty - its source's type definitions, externals and/or unused code got optimized away by ReScript.

describe('NextImpl.native', () => {
  it('skipped — see SKIP-REASON', () => {
  });
});
